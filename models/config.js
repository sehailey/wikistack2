const config = {
    database: 'wikistack',
    username: 'fsa',
    password: 'secretPassword123',
    options: {
        host: 'localhost',
        dialect: 'postgres'
    }
}

module.exports = config;
